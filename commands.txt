git init    //perdoret per me inicializu repositorin
git status -s //perdoret per me e pa files lokale a jane n staging area(zona testuse)
git add .   // add local files to staging area
git commit -m "Tituli"      // add from staging area to history 
git log --oneline               // shows all commits all history


git remote add origin https://github.com/floriansaqipi/github-basics.git //adds all things u need 